paste to terminal if you have git installed
```git clone https://github.com/fygen/theCVproject```

enter project dir
```cd theCVproject```

if npm installed then npm install and start again in cmd or bash any terminal.
```npm install```
```npm start ```
